<hc:Window x:Class="RPStesting.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:RPStesting" d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"

        Title="MainView" Height="450" Width="800" Background="DarkGray"  >
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid>
        <StackPanel>
            <TextBlock Text="Register Value:" Margin="10"/>
            <TextBox Text="{Binding RegisterValue, UpdateSourceTrigger=PropertyChanged}" Margin="10"/>

            <Button Content="Читать" Command="{Binding ReadCommand}" Margin="10"/>
            <Button Content="Писать" Command="{Binding WriteCommand}" Margin="10"/>
            <Button Content="Щелкать" Command="{Binding ToggleRegisterCommand}" Margin="10"/>

            <TextBlock Text="{Binding StatusMessage}" Margin="10"/>
        </StackPanel>
    </Grid>
</hc:Window>
